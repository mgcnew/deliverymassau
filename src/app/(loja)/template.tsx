/** Mesma transicao do painel, para a loja publica. Ver template do painel. */
export default function TemplateLoja({ children }: { children: React.ReactNode }) {
  return <div className="transicao-pagina">{children}</div>
}
